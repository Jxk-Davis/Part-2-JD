using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Home : MonoBehaviour
{
    //do nothing but wait for the player to touch you.
    //if the player touches you, check if they were carrying an item.
    //  if so... then add +1 to the score + some time to the timer, then make the player no longer holding an item

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
