using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SceneNameTxt : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
//add button to canvas
//shift alt select bottom right for the button
//button label to 'next scene'
//make button call function from ScneneCTRL
//just drag the object from the hierarchy into OnClick() and pick out the right function. you have done this before.